export interface User  {
  email: String;
  password: String;
}

export interface UserProfile {
  id: string;
  phoneNumber: string;
  firstName: string;
  lastName: string;
  address: string;
  birthDate: Date;
  photoURL: string;
  email: string;
  uid: string;
}
